Generate a self-contained p5.js instance-mode sketch for HermesVJ.

Creative priority:
- maximize beauty, fantasy atmosphere, depth, and display-readiness
- improve the prompt internally if it is plain or under-specified
- prefer cinematic composition and layered motion over literal depiction
- create a strong hero silhouette, not a generic center glow

Art-direction requirements:
- choose a restrained palette: dark base + 2 main hues + 1 accent at most
- establish foreground, midground, and background depth layers
- keep one dominant focal structure and let negative space support it
- use slow ambient motion plus one sharper accent motion layer
- reserve the brightest highlights for a small part of the frame
- favor asymmetry, occlusion, and parallax over evenly distributed elements
- prefer a silhouette, architectural frame, portal, shard, canopy, or creature-like form over a large centered orb
- make particles serve depth and motion, not act as default filler

Hard requirements:
- export `default function sketch(p)`
- use `p.createCanvas(p.windowWidth, p.windowHeight)` in `setup`
- keep the sketch responsive in `windowResized`
- no DOM access beyond p5 canvas creation
- no network calls, no remote assets, no eval, no dynamic imports
- keep it procedural and self-contained
- output code only

Quality bar:
- avoid muddy full-frame overlays
- avoid repeating equal-weight circles, rings, or ribbons with no hierarchy
- avoid rainbow drift unless explicitly requested
- avoid oversized moon/halo centerpieces unless the prompt explicitly calls for symmetry
- make the visual read well both up close and across the room

Optional runtime adaptation:
- if `window.__HERMES_VJ_RUNTIME` is available, use it to tune density, framing, and composition for portrait, landscape, square, ultrawide, and fullscreen display
- if the composition depends heavily on framing, produce separate variant files outside this prompt flow
