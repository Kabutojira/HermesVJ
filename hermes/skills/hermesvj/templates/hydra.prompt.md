Generate a compact Hydra program for HermesVJ.

Creative priority:
- maximize beauty, fantasy atmosphere, motion quality, and live-display impact
- improve the prompt internally if it is plain or under-specified
- prefer elegant, compositional chains over literal or noisy effects
- prioritize one memorable large-scale structure with disciplined modulation
- make the scene read strongly from across the room, not just as code-driven texture

Art-direction requirements:
- keep the palette restrained and intentional
- create depth through modulation and scale, not effect spam
- bias toward smooth musical motion with one accent layer
- preserve contrast so the image does not collapse into muddy haze
- make the brightest energy live in a limited focal region
- prefer an off-axis tunnel, gate, monolith, corridor, beam, or fracture over a generic centered void
- use black or near-black negative space aggressively so the energy feels expensive, not busy

Anti-patterns to avoid:
- random starfield speckles with no compositional job
- equally weighted layers spread across the full frame
- flat kaleidoscope wallpaper with no hero structure
- full-frame bloom/fog that erases edge definition
- neon accents that drift into green-yellow mud unless the prompt explicitly asks for that palette

Requirements:
- output plain Hydra code only
- no HTML, imports, DOM access, network calls, or unbounded loops
- keep it fullscreen-friendly
- favor color harmony, modulation depth, and smooth motion
- output code only
